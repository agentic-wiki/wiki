---
type: task
title: search command
status: done
priority: high
tags: [feature, query]
---

`wiki search <query>` over content + frontmatter values, case-insensitive by default. Honour `--type/--tag/--prefix` and add `--lines` (matching lines, grep-style). Reads from disk; does not need the link graph.

See also: [tags & properties](./003-tags-and-properties.md).
