---
type: task
title: link graph — links / backlinks / deadends
status: todo
priority: medium
tags: [feature, graph]
---

`wiki links <file>` (outgoing; `--resolve`), `wiki backlinks <file>` (incoming), `wiki deadends` (entries with no outgoing links). The index already holds the edge set used by `unresolved`/`orphans`; expose the remaining views.
