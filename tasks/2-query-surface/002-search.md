---
type: task
title: search command
status: todo
priority: high
tags: [feature, query]
---

`wiki search <query>` over content + frontmatter values, case-insensitive by default. Honour `--type/--tag/--path` and add `--context` (matching lines, grep-style). Reads from disk; does not need the link graph.

See also: [tags & properties](/2-query-surface/003-tags-and-properties.md).
