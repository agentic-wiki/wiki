---
okf_version: "0.1"
---

# Organization Knowledge Base

The home page. Link into each domain as you build it:

- [ ] Keep the domains you use, drop the rest (see WORKFLOW.md, "First run").
- [ ] Link each domain's `index.md` here (projects, clients, products, people, teams, ...).

---

_Operated with the `wiki` CLI: `AGENTS.md` is how to drive it, `WORKFLOW.md` is this base's conventions (both yours to edit)._
